[project]
name = "sc-bench"
version = "0.1.0"
description = "SC-bench (SupChain-Bench, ACL 2026) as a HUD v6 environment — bench-ception golden."
requires-python = ">=3.11,<3.13"
dependencies = ["hud-python>=0.6.0"]

[tool.uv]
package = false
